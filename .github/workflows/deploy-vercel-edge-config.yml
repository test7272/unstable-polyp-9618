name: Deploy to Vercel Edge Config
"on":
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy Edge Config
        run: |
          npx vercel edge-config push ./edge-config.json --token ${{ secrets.VERCEL_TOKEN }}
